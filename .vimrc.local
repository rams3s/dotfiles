set secure exrc
set visualbell

let project_root = getcwd()
let g:ctrlp_user_command = 'echo %s && find -L ' . project_root . ' -type f -not -path "*/.git/*" -not -path "*.meta" -not -path "*/metadata/*" -not -path "*/extern/*" -not -path "*/Export/*"'
